name: release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@ac593985615ec2ede58e132d2e21d2b1cbd6127c # pin@v2
      - uses: cli/gh-extension-precompile@5eff1a0e187503e04b6cf51da7691036bff2d210 # pin@v1
